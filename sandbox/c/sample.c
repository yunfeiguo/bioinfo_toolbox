#include <stdio.h>

short number,sum;
int bignum,bigsum;
char letter;

main()
{
    float x,y,z;
    char ch;
    x=y=z=3;
    typedef real float;
    typedef letter char;
    real sum=0.0;
    letter nextletter;
    printf ("%c %d %f",ch,i,x);
    scanf ("%c %d %f",&ch,&i,&x);
    int const a=1;
    const int a=2;
    void strcpy(char *buffer,char const *string)
}

int x,y,w;
main()
{
    x=((++z)-(w--))%100;
    z++;
    x=(z-w)%100;
    w--;
    x*=(z-w);
}
